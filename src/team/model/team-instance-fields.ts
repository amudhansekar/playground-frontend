// Fields shared between request and response
const idField = "id";

const nameField = "name";

const descriptionField = "description";

const attributesField = "attributes";

// Fields for request
const playerIdsField = "playerIds";

// Fields for response
const playersField = "players";

const scoreField = "score";

export {
  attributesField,
  descriptionField,
  idField,
  nameField,
  playerIdsField,
  playersField,
  scoreField,
};
